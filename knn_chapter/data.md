进行模型测试时，选用鸢尾花数据集，作为检验 KNN 模型的理想选择，其优势显著：样本量适中（150 个）且特征维度低（4 维），能让 KNN 在避免高维距离失效问题的同时，以较低计算成本完成预测，适合验证算法核心逻辑；3 个类别样本均衡（各 50 个），可避免类别不平衡干扰 KNN 的多数投票机制；特征为明确可量化的植物学测量值，类别间区分度清晰，能有效体现 KNN 基于距离度量进行相似性判断的优势，且数据无缺失值，无需复杂预处理即可直接使用，便于快速验证 KNN 在不同 K 值下的分类效果，是检验其 “惰性学习” 特性和距离敏感型分类逻辑的优良数据集。